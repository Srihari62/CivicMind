# Auth Feature Module

This directory contains auth-specific logic, state, and custom elements for CivicMind.
It encapsulates login, registration, password resets, and user role selection.

## Components & Utilities
To be populated:
- LoginForm/
- RegisterForm/
- ResetPasswordForm/
- AuthStateProvider
